"use client";

const LandingNav = () => {
  return (
    <nav className=" w-full h-20 justify-center items-center absolute bottom-0 lg:relative bg-black lg:flex ">
      LandingNav
    </nav>
  );
};

export default LandingNav;
